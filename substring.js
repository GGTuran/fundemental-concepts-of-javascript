const sub = "It is unfair but i need to do it";
const cut = sub.substring(0,12);  
console.log(cut);

// slice is also same
// but substring has more advantages while using
// 

// Padding
const num = "69";
const pad = num.padStart(8,'0');
console.log(pad)