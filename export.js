import greetold from "./function.js";
greetold("gg");
// 

// **namely export
//** */if we only use export we have to write that particular name of function which is exported and it will be in curly brace
//  * */ if we use export default it is independent

// ******